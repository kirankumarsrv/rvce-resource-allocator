name: Deploy to Kubernetes
permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'ECR image tag to deploy (for example full commit SHA or latest). Required for manual deploys.'
        required: true
      skip_smoke_test:
        description: 'Skip smoke tests during deployment'
        required: false
        default: 'false'
  workflow_run:
    workflows: ["Backend CI", "Frontend CI"]
    types: [completed]
    branches: [main]

jobs:
  check-all-passed:
    name: Verify All CI Workflows Passed
    runs-on: ubuntu-22.04
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    outputs:
      should-deploy: ${{ steps.check.outputs.result }}
      image_tag: ${{ steps.resolve-tag.outputs.value }}
    steps:
      - name: Resolve image tag
        id: resolve-tag
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_TAG: ${{ github.event.inputs.image_tag }}
          WORKFLOW_RUN_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          if [ "$EVENT_NAME" = "workflow_run" ] && [ -n "$WORKFLOW_RUN_SHA" ]; then
            TAG="$WORKFLOW_RUN_SHA"
          elif [ -n "$INPUT_TAG" ]; then
            TAG="$(printf '%s' "$INPUT_TAG" | xargs)"
          else
            echo "ERROR: image_tag input is required for manual workflow_dispatch."
            exit 1
          fi

          echo "value=$TAG" >> "$GITHUB_OUTPUT"
          echo "Resolved image tag: '$TAG'"

      - name: Check all workflows passed
        id: check
        run: |
          echo "Deploying commit: ${{ github.sha }}"
          echo "result=true" >> $GITHUB_OUTPUT

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-22.04
    needs: check-all-passed
    env:
      SKIP_SMOKE_TEST: ${{ github.event.inputs.skip_smoke_test || 'false' }}
    environment:
      name: staging
      url: https://staging.scas.rvce.edu.in

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (Assume role via OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ secrets.AWS_REGION }}
          audience: sts.amazonaws.com

      - name: Update kubeconfig for EKS
        env:
          AWS_TARGET_REGION: ${{ secrets.AWS_REGION }}
        run: |
          aws eks update-kubeconfig --region $AWS_TARGET_REGION --name scas-cluster

      - name: Deploy to staging namespace
        env:
          IMAGE_TAG: ${{ needs.check-all-passed.outputs.image_tag }}
          ECR_REGISTRY: ${{ secrets.ECR_REGISTRY }}
          NAMESPACE: scas-staging
        run: |
          kubectl apply -f infra/k8s/staging-backend-deployment.yaml -n ${NAMESPACE}
          kubectl apply -f infra/k8s/staging-frontend-deployment.yaml -n ${NAMESPACE}

          kubectl set image deployment/scas-backend \
            backend="${ECR_REGISTRY}/scas-backend:${IMAGE_TAG}" \
            -n ${NAMESPACE}

          kubectl set image deployment/scas-frontend \
            frontend="${ECR_REGISTRY}/scas-frontend:${IMAGE_TAG}" \
            -n ${NAMESPACE}

      - name: Scale staging deployments to 1 for rollout
        run: |
          kubectl scale deployment/scas-backend deployment/scas-frontend --replicas=1 -n scas-staging
          kubectl get deploy scas-backend scas-frontend -n scas-staging

      - name: Wait for rollout to complete
        run: |
          set +e
          kubectl rollout status deployment/scas-backend -n scas-staging --timeout=5m
          BACKEND_STATUS=$?
          kubectl rollout status deployment/scas-frontend -n scas-staging --timeout=5m
          FRONTEND_STATUS=$?
          if [ $BACKEND_STATUS -ne 0 ] || [ $FRONTEND_STATUS -ne 0 ]; then
            echo "Rollout failed or timed out. Gathering diagnostics..."
            kubectl get pods -n scas-staging -o wide
            kubectl describe deployment scas-backend scas-frontend -n scas-staging
            kubectl get events -n scas-staging --sort-by='.metadata.creationTimestamp' | tail -n 50
            exit 1
          fi

      - name: Ensure staging ingress exists
        run: |
          kubectl apply -f infra/k8s/staging-ingress.yaml
          kubectl get ingress -n scas-staging
          kubectl describe ingress scas-staging-ingress -n scas-staging || true

      - name: Run smoke tests against staging
        if: ${{ env.SKIP_SMOKE_TEST != 'true' }}
        run: |
          STAGING_URL="https://staging.scas.rvce.edu.in"
          sleep 30

          echo "Checking staging cluster resources..."
          kubectl get pods -n scas-staging || true
          kubectl get svc -n scas-staging || true
          kubectl get ingress -n scas-staging || true
          kubectl describe ingress scas-staging-ingress -n scas-staging || true

          echo "Resolving staging host..."
          getent hosts staging.scas.rvce.edu.in || true

          HTTP_STATUS=$(curl -sS --retry 10 --retry-delay 15 --retry-connrefused --max-time 30 \
            -o /tmp/staging-health.txt -w "%{http_code}" "${STAGING_URL}/actuator/health" || true)

          echo "HTTP_STATUS=${HTTP_STATUS}"
          cat /tmp/staging-health.txt || true

          if [ "$HTTP_STATUS" != "200" ]; then
            echo "Smoke test FAILED: health check returned ${HTTP_STATUS}"
            exit 1
          fi

          echo "Smoke test PASSED: health check returned 200"

      - name: Skip staging smoke test
        if: ${{ env.SKIP_SMOKE_TEST == 'true' }}
        run: echo "Skipping staging smoke test because skip_smoke_test=true"

      - name: Validate staging image tag
        run: |
          if [ -z "${IMAGE_TAG}" ]; then
            echo "ERROR: IMAGE_TAG is empty. Deployment cannot continue."
            exit 1
          fi
          echo "Deploying staging images with tag: '${IMAGE_TAG}'"

      - name: Notify on success
        if: success()
        run: |
          echo "Deployment to staging succeeded: ${{ github.sha }}"

      - name: Rollback on failure
        if: failure()
        run: |
          echo "Deployment FAILED. Rolling back..."
          kubectl rollout undo deployment/scas-backend -n scas-staging
          kubectl rollout undo deployment/scas-frontend -n scas-staging
          echo "Rollback complete"

  deploy-production:
    name: Deploy to Production (Manual Approval)
    runs-on: ubuntu-22.04
    needs: deploy-staging
    env:
      SKIP_SMOKE_TEST: ${{ github.event.inputs.skip_smoke_test || 'false' }}
    environment:
      name: production
      url: https://scas.rvce.edu.in

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (Assume role via OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ secrets.AWS_REGION }}
          audience: sts.amazonaws.com

      - name: Update kubeconfig
        env:
          AWS_TARGET_REGION: ${{ secrets.AWS_REGION }}
        run: aws eks update-kubeconfig --region $AWS_TARGET_REGION --name scas-cluster

      - name: Deploy to production namespace
        env:
          IMAGE_TAG: ${{ needs.check-all-passed.outputs.image_tag }}
          ECR_REGISTRY: ${{ secrets.ECR_REGISTRY }}
          NAMESPACE: scas-prod
        run: |
          if [ -z "${IMAGE_TAG}" ]; then
            echo "ERROR: IMAGE_TAG is empty. Production deployment cannot continue."
            exit 1
          fi
          echo "Deploying production images with tag: '${IMAGE_TAG}'"

          kubectl apply -f infra/k8s/production-backend-deployment.yaml -n ${NAMESPACE}
          kubectl apply -f infra/k8s/production-frontend-deployment.yaml -n ${NAMESPACE}

          kubectl set image deployment/scas-backend \
            backend="${ECR_REGISTRY}/scas-backend:${IMAGE_TAG}" \
            -n ${NAMESPACE}

          kubectl set image deployment/scas-frontend \
            frontend="${ECR_REGISTRY}/scas-frontend:${IMAGE_TAG}" \
            -n ${NAMESPACE}

      - name: Ensure production deployments are scaled up
        run: |
          kubectl scale deployment/scas-backend scas-frontend --replicas=1 -n scas-prod
          kubectl get deploy scas-backend scas-frontend -n scas-prod

      - name: Wait for production rollout
        run: |
          kubectl rollout status deployment/scas-backend -n scas-prod --timeout=5m
          kubectl rollout status deployment/scas-frontend -n scas-prod --timeout=5m

      - name: Ensure production ingress exists
        run: |
          kubectl apply -f infra/k8s/production-ingress.yaml
          kubectl get ingress -n scas-prod
          kubectl describe ingress scas-production-ingress -n scas-prod || true

      - name: Production smoke test
        if: ${{ env.SKIP_SMOKE_TEST != 'true' }}
        run: |
          PRODUCTION_URL="https://scas.rvce.edu.in"

          echo "Resolving production host..."
          getent hosts scas.rvce.edu.in || true

          HTTP_STATUS=$(curl -sS --retry 10 --retry-delay 15 --retry-connrefused --max-time 30 \
            -o /tmp/production-health.txt -w "%{http_code}" "${PRODUCTION_URL}/actuator/health" || true)

          echo "HTTP_STATUS=${HTTP_STATUS}"
          cat /tmp/production-health.txt || true
          [ "$HTTP_STATUS" = "200" ] || exit 1

      - name: Skip production smoke test
        if: ${{ env.SKIP_SMOKE_TEST == 'true' }}
        run: echo "Skipping production smoke test because skip_smoke_test=true"

      - name: Rollback production on failure
        if: failure()
        run: |
          kubectl rollout undo deployment/scas-backend -n scas-prod
          kubectl rollout undo deployment/scas-frontend -n scas-prod
